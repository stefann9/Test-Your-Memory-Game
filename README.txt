Game logic:

Generates a set of numbers in ASC order.
The nums are added in .container with random positions
After 3s the nums are hidden.
Player must choose the nums in asc order
If succeeds, a new set +1 of nums are added.

